function sketch3(p) {
    p.setup = function () {
        p.createCanvas(450, 450);
        p.strokeWeight(2);
        p.background(0)


        for(let y = 0; y < p.height; y += 55) {
            for(let x = 0; x < p.width; x += 55) {

            };
        };
    };

}

new p5(sketch3);
